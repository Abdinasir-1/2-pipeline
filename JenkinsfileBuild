pipeline {
	agent any
	stage {
		stage('Build Images') {
				step {
					sh "./scripts/build_images.sh"
				     }
		}
		stage('Push Images') {
				steps {
					sh "./scripts/push_images.sh"
				      }
		}
	}
}
