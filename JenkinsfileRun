pipeline {
	agent any
	stage {
		stage('Pull Images') {
				step {
					sh "./scripts/pull_images.sh"
				     }
		}
		stage('Run Container') {
				steps {
					sh "./scripts/run.sh"
				      }
		}
	}
}
